#include<stdio.h>


long long maxSum(int* nums, int numsSize, int m, int k) {
	int left = 0, right = k - 1;//左指针和右指针
	int count = 0, Maxcount = 0, temp = 0;
	
	int* arr = (int*)malloc(sizeof(int) * k);

	
}